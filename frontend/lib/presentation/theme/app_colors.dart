import 'package:flutter/material.dart';

class AppColors {
  // Backgrounds
  static const Color oledBlack = Color(0xFF000000);
  static const Color background = Color(0xFF08080F);
  static const Color cardBg = Color(0xFF111118);
  
  // Primary / Glows
  static const Color indigo = Color(0xFF4A6BFF);
  static const Color indigoLight = Color(0xFF8E9BFF);
  static const Color orange = Color(0xFFFF9500);
  
  // Feedback
  static const Color green = Color(0xFF30D158);
  static const Color red = Color(0xFFFF453A);

  // Borders
  static const Color glassBorder = Color(0x1AFFFFFF); // 10% white
  static const Color cardBorder = Color(0xFF1E1E2E);
}
